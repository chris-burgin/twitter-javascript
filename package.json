{
  "name": "javascript-twitter",
  "version": "0.2.0",
  "main": "main.js",
  "scripts": {
    "pack": "npm run pack:osx",
    "pack:osx": "npm run build:osx && electron-builder \"./LooplineSystems.app\" --platform=osx --config=config.json"
  },
  "devDependencies": {
    "electron-packager": "^5.1.1"
  }
}
